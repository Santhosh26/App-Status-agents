{
  "name": "status-agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "deploy": "wrangler deploy && ([ -f .env.secrets ] && wrangler secret bulk .env.secrets || true)",
    "db:init": "wrangler d1 execute status-agent-db --local --file=schema.sql"
  },
  "dependencies": {
    "agents": "^0.7.2",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250312.0",
    "wrangler": "^4.10.0"
  }
}
